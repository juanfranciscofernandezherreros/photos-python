"""
photos_sync — pipeline to extract, organize, and compress screenshots
from an Android phone mounted via WebDAV.

Install the project in editable mode:
    pip install -e .

Run the pipeline:
    photos-sync --all
    photos-sync            # opens the interactive menu
"""

__version__ = "0.1.0"
