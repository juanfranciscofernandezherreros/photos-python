"""
photos_sync — pipeline para extraer, organizar y comprimir capturas de
pantalla desde un teléfono Android montado por WebDAV.

Instala el proyecto en modo editable con:
    pip install -e .

Y ejecuta el pipeline con:
    photos-sync --todo
    photos-sync            # abre el menú interactivo
"""

__version__ = "0.1.0"
