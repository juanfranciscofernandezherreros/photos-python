name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-analysis:
    name: Static analysis (${{ matrix.name }})
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ruff
            command: python -m ruff check photos_sync tests scripts
          - name: mypy
            command: python -m mypy photos_sync --ignore-missing-imports

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project and quality tools
        run: python -m pip install --disable-pip-version-check -e ".[dev]"

      - name: Run ${{ matrix.name }}
        run: ${{ matrix.command }}

  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install project and test tools
        run: python -m pip install --disable-pip-version-check -e ".[dev]"

      - name: Run tests
        run: python -m pytest tests -q --tb=short

  coverage:
    name: Coverage (minimum 80%)
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project and test tools
        run: python -m pip install --disable-pip-version-check -e ".[dev]"

      - name: Run branch coverage quality gate
        run: >-
          python -m pytest tests -q --tb=short
          --cov=photos_sync --cov-branch
          --cov-report=term-missing --cov-report=html --cov-report=xml
          --cov-fail-under=80

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: python-coverage
          path: |
            reports/coverage/
            reports/coverage.xml
          retention-days: 14

  serenity:
    name: Cucumber API contracts and Serenity report
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: serenity/pom.xml

      - name: Install the disposable API
        run: python -m pip install --disable-pip-version-check -e ".[dev]"

      - name: Exercise every endpoint and aggregate Serenity HTML
        working-directory: serenity
        run: bash ./mvnw --batch-mode verify

      - name: Verify request and response evidence
        run: python scripts/verify_serenity_report.py

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: serenity-api-report
          path: serenity/target/site/serenity/
          retention-days: 14

  postgres-integration:
    name: PostgreSQL 16 migrations and restore
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: photos_sync_ci
          POSTGRES_USER: photos
          POSTGRES_PASSWORD: photos-sync-ci-only
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U photos -d photos_sync_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql://photos:photos-sync-ci-only@127.0.0.1:5432/photos_sync_ci
      PGHOST: 127.0.0.1
      PGPORT: "5432"
      PGUSER: photos
      PGPASSWORD: photos-sync-ci-only
      PGDATABASE_ADMIN: postgres

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project and PostgreSQL client
        run: |
          python -m pip install --disable-pip-version-check -e ".[dev]"
          sudo apt-get update
          sudo apt-get install --yes postgresql-client

      - name: Verify Alembic schema, PostgreSQL types, and constraints
        run: python scripts/verify_postgres.py

      - name: Verify a compressed backup can be restored
        run: sh scripts/test_backup_restore.sh

  build:
    name: Build and smoke test
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 15
    needs: [static-analysis, test, coverage, serenity, postgres-integration]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install build tool
        run: python -m pip install --disable-pip-version-check build

      - name: Build wheel and source distribution
        run: python -m build

      - name: Install wheel in a clean environment
        run: |
          python -m venv .venv-build
          .venv-build/bin/python -m pip install --disable-pip-version-check --upgrade pip
          .venv-build/bin/python -m pip install "$(find dist -maxdepth 1 -name '*.whl' -print -quit)[ssh,images]"

      - name: Smoke-test CLI entry points
        run: |
          .venv-build/bin/photos-sync --help
          .venv-build/bin/photos-sync-web --help

      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 14

  ci-gate:
    name: CI gate
    runs-on: [self-hosted, linux, x64]
    if: always()
    needs: [static-analysis, test, coverage, serenity, postgres-integration, build]
    steps:
      - name: Require every CI stage to pass
        env:
          STATIC_RESULT: ${{ needs.static-analysis.result }}
          TEST_RESULT: ${{ needs.test.result }}
          COVERAGE_RESULT: ${{ needs.coverage.result }}
          SERENITY_RESULT: ${{ needs.serenity.result }}
          POSTGRES_RESULT: ${{ needs.postgres-integration.result }}
          BUILD_RESULT: ${{ needs.build.result }}
        run: |
          test "$STATIC_RESULT" = success
          test "$TEST_RESULT" = success
          test "$COVERAGE_RESULT" = success
          test "$SERENITY_RESULT" = success
          test "$POSTGRES_RESULT" = success
          test "$BUILD_RESULT" = success
